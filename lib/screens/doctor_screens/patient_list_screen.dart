import 'package:flutter/material.dart';

class PatientListScreen extends StatelessWidget {
  static String id = 'patient_list_screen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Patient List Screen'),
    );
  }
}