import 'package:flutter/material.dart';

class DoctorProfileScreen extends StatelessWidget {
  static String id = 'doctor_profile_screen';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Doctor Profile Screen'),
    );
  }
}